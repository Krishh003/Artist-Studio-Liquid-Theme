{% comment %}
  Artist Artworks Grid
  - CSS columns masonry layout (not CSS grid)
  - Artworks sourced from the artist's automated collection
  - Cards open artwork modal via .artwork-card delegation in artist-content.js
{% endcomment %}

{%- liquid
  assign artist = metaobject
  assign artist_handle = artist.handle
  assign artworks = collections[artist_handle].products
  if artworks == blank or artworks.size == 0
    comment
      Fallback: filter all products by tag. Slower than an automated collection.
      Create a per-artist automated collection (handle = artist handle, condition:
      "Product tag equals artist:{handle}") to use the faster primary path above.
    endcomment
    assign tag_filter = 'artist:' | append: artist_handle
    assign artworks = collections.all.products | where: 'tags', tag_filter
  endif
-%}

<section class="bg-background-light-accent dark:bg-background-accent" style="padding: 6rem 0;">

  <div class="container" style="max-width: 80rem; margin: 0 auto; padding: 0 2rem;">

    <!-- Section header -->
    <div style="display: flex; align-items: center; justify-content: space-between; margin-bottom: 3rem;">
      <h3 style="font-size: 0.75rem; font-weight: 700; letter-spacing: 0.2em;
                 text-transform: uppercase; color: #94a3b8;" class="reveal">
        Selected Works
      </h3>
    </div>

    <!-- DEBUG: remove before deploy -->
    <p style="font-family:monospace;font-size:0.75rem;color:#f00;padding:1rem 0;">
      artist_handle={{ artist_handle }} |
      collection={{ collections[artist_handle].title | default: 'NOT FOUND' }} |
      artworks.size={{ artworks.size }}
    </p>

    <!-- CSS columns masonry -->
    <div class="artworks-masonry">
      {% for product in artworks limit: 50 %}
        {% render 'artwork-card', product: product, index: forloop.index0 %}
      {% else %}
        <p style="text-align: center; padding: 6rem 0; color: rgba(0,0,0,0.2);
                  font-family: var(--font-serif); font-size: 1.5rem; font-style: italic;">
          No artworks listed yet.
        </p>
      {% endfor %}
    </div>

  </div>
</section>

{% schema %}
{
  "name": "Artist Artworks",
  "settings": [],
  "presets": [
    {
      "name": "Artist Works Grid"
    }
  ]
}
{% endschema %}
