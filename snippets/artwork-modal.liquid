<div id="artwork-modal" class="artwork-modal-overlay">

  <!-- Close Button -->
  <button id="modal-close" class="artwork-modal-close" aria-label="Close">
    <svg width="28" height="28" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5">
      <path d="M18 6L6 18M6 6l12 12"/>
    </svg>
  </button>

  <!-- Image pane -->
  <div class="artwork-modal-image-pane">
    <img id="modal-image" src="" alt="" width="1200" height="900" class="artwork-modal-img" data-colorthief-target crossorigin="anonymous">
  </div>

  <!-- Detail pane -->
  <div class="artwork-modal-detail reveal-modal">
    <span class="artwork-modal-eyebrow">Viewing Piece</span>
    <h2 id="modal-title" class="artwork-modal-title"></h2>
    <p id="modal-artist-name" class="artwork-modal-artist"></p>
    <div class="artwork-modal-rule dynamic-accent-bg"></div>
    <p id="modal-desc" class="artwork-modal-desc"></p>
    <div class="artwork-modal-actions">
      <button class="artwork-modal-inquire-btn" data-inquiry-button>
        Inquire Details
      </button>
    </div>
  </div>

</div>

<style>
  /* ---- Full-screen overlay ---- */
  .artwork-modal-overlay {
    display: none;
    position: fixed;
    inset: 0;
    z-index: 500;
    background: rgba(0, 0, 0, 0.97);
    flex-direction: row;
    align-items: center;
    overflow-y: auto;
  }

  .artwork-modal-overlay.is-open {
    display: flex;
    animation: modal-fade-in 0.35s ease forwards;
  }

  @keyframes modal-fade-in {
    from { opacity: 0; }
    to   { opacity: 1; }
  }

  /* ---- Close button ---- */
  .artwork-modal-close {
    position: absolute;
    top: 2rem;
    right: 2rem;
    z-index: 510;
    background: none;
    border: none;
    cursor: pointer;
    color: rgba(255, 255, 255, 0.7);
    padding: 0.5rem;
    transition: color 0.2s, transform 0.25s;
  }
  .artwork-modal-close:hover {
    color: #fff;
    transform: rotate(90deg);
  }

  /* ---- Image pane (left, ~58%) ---- */
  .artwork-modal-image-pane {
    flex: 0 0 58%;
    max-width: 58%;
    display: flex;
    align-items: center;
    justify-content: center;
    padding: 4rem 3rem;
    height: 100vh;
    box-sizing: border-box;
  }

  .artwork-modal-img {
    max-width: 100%;
    max-height: 75vh;
    object-fit: contain;
    display: block;
    box-shadow: 0 25px 60px rgba(0, 0, 0, 0.6);
    border-radius: 2px;
    transition: opacity 0.4s ease;
  }

  /* ---- Detail pane (right, ~42%) ---- */
  .artwork-modal-detail {
    flex: 1;
    display: flex;
    flex-direction: column;
    justify-content: center;
    padding: 4rem 4rem 4rem 2rem;
    color: #fff;
    /* slide-in handled by reveal-modal classes */
  }

  .artwork-modal-eyebrow {
    display: block;
    font-family: var(--font-sans, sans-serif);
    font-size: 0.65rem;
    font-weight: 700;
    letter-spacing: 0.3em;
    text-transform: uppercase;
    color: rgba(255, 255, 255, 0.5);
    margin-bottom: 1rem;
  }

  .artwork-modal-title {
    font-family: var(--font-serif);
    font-size: clamp(2.25rem, 4.5vw, 4rem);
    font-weight: 400;
    line-height: 1.1;
    color: #fff;
    margin: 0 0 0.75rem 0;
  }

  .artwork-modal-artist {
    font-family: var(--font-sans, sans-serif);
    font-size: 0.7rem;
    font-weight: 600;
    letter-spacing: 0.25em;
    text-transform: uppercase;
    color: rgba(255, 255, 255, 0.45);
    margin: 0 0 2rem 0;
  }

  .artwork-modal-rule {
    width: 3rem;
    height: 1px;
    background: var(--gold-primary, #b8965a);
    margin-bottom: 1.75rem;
  }

  .artwork-modal-desc {
    font-family: var(--font-sans, sans-serif);
    font-size: 0.95rem;
    line-height: 1.7;
    color: rgba(255, 255, 255, 0.5);
    margin: 0 0 2.5rem 0;
    max-width: 36ch;
  }

  .artwork-modal-actions {
    display: flex;
    gap: 1rem;
  }

  .artwork-modal-inquire-btn {
    background: transparent;
    color: #fff;
    border: 1px solid rgba(255, 255, 255, 0.6);
    border-radius: 9999px;
    padding: 0.85rem 2.25rem;
    font-family: var(--font-sans, sans-serif);
    font-size: 0.7rem;
    font-weight: 600;
    letter-spacing: 0.2em;
    text-transform: uppercase;
    cursor: pointer;
    transition: background 0.2s, border-color 0.2s, color 0.2s;
  }
  .artwork-modal-inquire-btn:hover {
    background: rgba(255, 255, 255, 0.1);
    border-color: #fff;
  }

  /* ---- reveal-modal slide-in ---- */
  .reveal-modal {
    opacity: 0;
    transform: translateX(2rem);
    transition: opacity 0.45s ease, transform 0.45s ease;
  }
  .reveal-modal--active {
    opacity: 1;
    transform: translateX(0);
  }

  /* ---- Mobile stacked layout ---- */
  @media (max-width: 768px) {
    .artwork-modal-overlay {
      flex-direction: column;
      align-items: stretch;
    }
    .artwork-modal-image-pane {
      flex: none;
      max-width: 100%;
      height: auto;
      padding: 5rem 1.5rem 1.5rem;
    }
    .artwork-modal-img {
      max-height: 45vh;
    }
    .artwork-modal-detail {
      padding: 1.5rem 1.5rem 3rem;
    }
  }
</style>
